# Feature Candidates

Generated feature proposals live here as metadata-only YAML.

Feature candidates are not active sparse features. They must remain proposals until a human review queue marks them `accepted`.

Do not include raw Threads text, URLs, handles, screenshots, personal identifiers, browser artifacts, credentials, or controlled-store paths.

Current manually proposed candidates:

- `context_required_result_display.yaml`: pending Batch 0007 validation; do not
  promote until a human queue decision is `accepted`.
