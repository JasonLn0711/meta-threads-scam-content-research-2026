# Exploration

This directory stores metadata-only exploration tasks.

The exploration layer proposes where to look next; it does not collect external content, crawl, store raw Threads evidence, include PII, or make final decisions.
