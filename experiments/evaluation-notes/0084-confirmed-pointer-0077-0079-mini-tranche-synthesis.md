# Confirmed Pointer 0077-0079 Mini-Tranche Synthesis

## Purpose

Synthesize the three recent stakeholder-confirmed pointers that have preliminary top-level rule extraction but still require controlled full-thread/reply capture before item promotion.

This note is repo-safe. It does not include raw Threads URLs, handles, screenshots, raw post text, raw reply text, contact IDs, stock names, stock codes, price values, credentials, browser/session artifacts, exact controlled-store paths, or stakeholder case IDs.

## Status Summary

| Item | Decision | Run record | Preliminary rule family | Manual entry | Full-thread status |
|---|---|---|---|---|---|
| `0077` | `0097` | `0046` | `supply_chain_insider_stock_lure` | not built | pending |
| `0078` | `0098` | `0047` | `hidden_stock_code_past_performance_lure` | not built | pending |
| `0079` | `0099` | `0048` | `hidden_stock_code_past_performance_lure` + `comment_code_lead_magnet` | not built | pending |

## What Has Been Learned

The three pointers suggest a cluster of finance-scam persuasion patterns that do not necessarily expose public contact handles or external links in the top-level post.

Observed repo-safe rule learning:

- claimed insider/supply-chain access can be used as authority for stock-buying pressure;
- prior buy/sell performance proof can establish trading authority before teasing the next hidden stock/code;
- public comment-keyword gates can act as a lead magnet even before any visible private-channel link appears;
- full reply/comment capture is needed before deciding whether the public lure becomes a private-message, contact-handle, group, or individualized-advice funnel.

## Current Limitation

Static public metadata recovered only top-level post-level signals. It did not provide reliable full reply/comment capture.

Therefore:

- no `manual_entry_0077.json` exists;
- no `manual_entry_0078.json` exists;
- no `manual_entry_0079.json` exists;
- no `manual_record_0077.json`, `manual_record_0078.json`, or `manual_record_0079.json` exists;
- no strict validation applies to these prospective items yet.

## Authorized Next Gate

Decision `0100` opens work order `0049` for controlled full-thread capture attempts on items `0077-0079` only.

Promotion remains blocked until each item completes:

1. controlled full-thread/reply capture or explicit unavailability note;
2. redacted reply/comment summary;
3. second review;
4. manual entry build decision;
5. strict validation if a manual record is built.

## Decision Implication

Treat `0077-0079` as a mini-tranche of preliminary rule learning, not as a completed checkpoint extension.

Do not start item `0080`, broad browser search, crawler expansion, account graph capture, embedding/model training, production detection, or legal fraud determination from this synthesis.
